---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);
---

<BaseLayout title="Blog" description="Articles on industrial automation, PLC programming, SCADA systems, and controls engineering.">
  <section class="bg-primary-dark text-white py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl">
        <h1 class="text-4xl md:text-5xl font-extrabold mb-6">Blog</h1>
        <p class="text-xl text-gray-300 leading-relaxed">
          Thoughts on industrial automation, controls engineering, and software development.
        </p>
      </div>
    </div>
  </section>

  <section class="py-20">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      {posts.length === 0 ? (
        <div class="text-center py-16">
          <div class="text-5xl mb-4">&#x1F4DD;</div>
          <h2 class="text-2xl font-bold text-primary-dark mb-3">Coming Soon</h2>
          <p class="text-text-light max-w-md mx-auto">
            Articles on industrial automation, protocol development, and software engineering are on the way.
            Check back soon.
          </p>
        </div>
      ) : (
        <div class="space-y-8">
          {posts.map((post) => (
            <article class="bg-white rounded-xl border border-gray-100 p-8 hover:shadow-md transition-shadow">
              <div class="flex items-center gap-3 text-sm text-text-light mb-3">
                <time datetime={post.data.date.toISOString()}>
                  {post.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
                </time>
                {post.data.tags && (
                  <>
                    <span>·</span>
                    <div class="flex gap-2">
                      {post.data.tags.map((tag: string) => (
                        <span class="bg-gray-100 px-2 py-0.5 rounded text-xs">{tag}</span>
                      ))}
                    </div>
                  </>
                )}
              </div>
              <h2 class="text-xl font-bold text-primary-dark mb-2">
                <a href={`/blog/${post.id}`} class="hover:text-accent transition-colors">
                  {post.data.title}
                </a>
              </h2>
              <p class="text-text-light">{post.data.description}</p>
              <a href={`/blog/${post.id}`} class="inline-block mt-4 text-accent font-medium text-sm hover:text-primary transition-colors">
                Read more &rarr;
              </a>
            </article>
          ))}
        </div>
      )}
    </div>
  </section>
</BaseLayout>
